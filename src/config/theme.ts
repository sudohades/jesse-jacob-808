/**
 * Theme Configuration
 * Theme selection and appearance
 */
export const theme = {
  mode: "dark" as const,
  colors: {
    primary: "#110C1D",
    accent: "#8B5CF6",
  },
  fonts: {
    display: "Quantico",
    sans: "Inter",
    mono: "Share Tech Mono",
  },
} as const;
