/**
 * Framework-independent site metadata configuration
 */
import type { SiteMetadata } from "./types";
import { metadata as configMetadata } from "@/config";

export const siteMetadata: SiteMetadata = configMetadata;
